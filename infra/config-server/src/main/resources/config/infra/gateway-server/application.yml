server:
  port: 8080

spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: user_service
          uri: lb://user-service
          predicates:
            - Path=/admin/users/**

        - id: category_service
          uri: lb://category-service
          predicates:
            - Path=/categories/**, /admin/categories/**

        - id: main_service
          uri: lb://main-service
          predicates:
            - Path=/**
#          filters:
#            - PrefixPath=/api/v1
